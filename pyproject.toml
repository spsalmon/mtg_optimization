[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "neoform"
version = "0.1.0"

dependencies = [
    "numpy",
    "seaborn",
    "joblib",
    "tqdm",
    "jupyter",
    "ipykernel",
    "polars",
]